[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Disable Ruby processing
[build.processing]
  skip_processing = false

# Environment variables
[context.production.environment]
  NODE_ENV = "production"